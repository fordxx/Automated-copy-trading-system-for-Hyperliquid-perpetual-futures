# 自己也在Hyperliquid交易的说明

## 场景描述

如果你自己也在Hyperliquid上开单，同时又想跟单其他地址的交易，这是一个常见的场景。本文档解释如何正确配置以避免问题。

## 🔍 工作原理

Hyperliquid的 `user_fills` API 只返回指定地址的交易记录：

```python
# 只返回 target_address 的交易，不会返回其他地址的交易
user_fills = self.info_client.user_fills([target_address])
```

**这意味着：**
- ✅ 你的交易不会被系统监控到（除非 target_address 是你的地址）
- ✅ 不会跟单自己的交易
- ✅ 系统只会响应目标地址的交易

## ⚙️ 配置建议

### 场景1: 跟单其他地址，自己也在交易

```yaml
# 目标地址 - 你想跟单的地址
target_address: "0x大师地址"

# 排除地址 - 可选，用于额外过滤
exclude_addresses: []
```

**结果：** 系统只会监控和跟单"大师地址"的交易，你的交易不会被影响。

### 场景2: 跟单多个地址（高级用法）

```yaml
# 目标地址 - 主要跟单地址
target_address: "0x主要地址"

# 排除地址 - 排除不需要的地址
exclude_addresses:
  - "0x你的地址"
  - "0x其他不需要的地址"
```

## 🚨 重要提醒

### 1. 不要跟单自己的地址
```yaml
# ❌ 错误的配置
target_address: "0x你的地址"  # 不要这样做！
```

跟单自己的交易没有意义，且可能导致意外的重复交易。

### 2. 资金管理
- 确保账户有足够资金同时支持：
  - 自己的手动交易
  - 系统的自动跟单交易
- 设置合理的 `max_position_size` 限制

### 3. 杠杆一致性
- 系统会复制目标地址的杠杆设置
- 确保你的账户支持相应的杠杆倍数

## 📊 风险控制

当你自己也在交易时，建议的配置：

```yaml
copy_trading:
  copy_ratio: 0.01       # 从1%开始
  max_position_size: 0.1 # 限制单笔最大仓位

risk_management:
  max_drawdown: 0.05     # 5%最大回撤
  stop_loss_ratio: 0.03  # 3%止损
```

## 🔍 监控建议

1. **实时监控**: 开启Telegram通知，实时了解系统行为
2. **定期检查**: 查看交易记录，确保没有异常
3. **资金监控**: 关注账户余额和仓位变化
4. **日志分析**: 检查系统日志，了解交易触发情况

## ❓ 常见问题

**Q: 会不会跟单到自己的交易？**
A: 不会。`user_fills` API 只返回指定目标地址的交易。

**Q: 如果我手动交易，会不会触发跟单？**
A: 不会。只有目标地址的交易才会触发跟单。

**Q: 如何停止跟单但继续手动交易？**
A: 设置 `copy_trading.enabled: false` 或停止程序。

**Q: 我的手动交易会影响系统判断吗？**
A: 不会。系统只监控目标地址，不关心你的账户状态（除了资金充足性）。

## 💡 最佳实践

1. **从小额开始**: 先用小比例测试系统稳定性
2. **分开账户**: 考虑用不同的账户进行手动交易和自动跟单
3. **定期评估**: 定期评估跟单策略的有效性
4. **保持警惕**: 市场变化快，及时调整策略

通过正确配置，你可以安全地在Hyperliquid上同时进行手动交易和自动跟单！🎯