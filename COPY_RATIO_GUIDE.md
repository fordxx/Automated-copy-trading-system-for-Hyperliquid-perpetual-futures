# 跟单比例说明

## 什么是跟单比例？

跟单比例 (`copy_ratio`) 是指**你的跟单仓位大小**相对于**目标地址交易大小**的比例。

## 计算公式

```
你的跟单大小 = 目标交易大小 × 跟单比例
```

然后会按以下顺序对“计划数量”做限制/调整：

1) 最大仓位限制（合约数量上限）

```
实际跟单大小 = min(你的跟单大小, 最大仓位大小)
```

2) （可选）单笔名义金额上限（USD）

如果设置了 `MAX_NOTIONAL_PER_TRADE_USD > 0`，会按成交价估算名义金额并进一步截断：

```
名义上限对应数量 = MAX_NOTIONAL_PER_TRADE_USD / 成交价
实际跟单大小 = min(实际跟单大小, 名义上限对应数量)
```

3) 合约精度向下取整（`szDecimals`）

交易所对不同币种的合约数量精度有限制，程序会按 `szDecimals` 向下取整。
例如 `szDecimals=0` 表示必须是整数张。

4) 最小下单数量

如果最终数量 `< MIN_TRADE_SIZE`（默认 0.01），程序会跳过该交易。

## 举例说明

### 场景1: 目标地址开仓 1.0 BTC多头

**配置:**
```yaml
copy_ratio: 0.1        # 10%跟单比例
max_position_size: 0.5 # 最大0.5 BTC
```

**计算:**
```
你的跟单大小 = 1.0 × 0.1 = 0.1 BTC
实际跟单大小 = min(0.1, 0.5) = 0.1 BTC
```

**结果:** 你会开仓 0.1 BTC多头 (目标的10%)

### 场景2: 目标地址开仓 0.05 BTC多头

**配置:**
```yaml
copy_ratio: 0.5        # 50%跟单比例
max_position_size: 0.1 # 最大0.1 BTC
```

**计算:**
```
你的跟单大小 = 0.05 × 0.5 = 0.025 BTC
实际跟单大小 = min(0.025, 0.1) = 0.025 BTC
```

**结果:** 你会开仓 0.025 BTC多头 (目标的50%)

### 场景3: 目标地址开仓 5.0 BTC多头 (大单)

**配置:**
```yaml
copy_ratio: 0.1        # 10%跟单比例
max_position_size: 0.2 # 最大0.2 BTC
```

**计算:**
```
你的跟单大小 = 5.0 × 0.1 = 0.5 BTC
实际跟单大小 = min(0.5, 0.2) = 0.2 BTC
```

**结果:** 你会开仓 0.2 BTC多头 (受最大仓位限制)

## 推荐设置

### 保守策略 (推荐新手)
```yaml
copy_ratio: 0.01       # 1%跟单比例
max_position_size: 0.1 # 最大仓位
```

### 平衡策略
```yaml
copy_ratio: 0.05       # 5%跟单比例
max_position_size: 0.5 # 最大仓位
```

### 激进策略 (高风险)
```yaml
copy_ratio: 0.2        # 20%跟单比例
max_position_size: 2.0 # 最大仓位
```

## 注意事项

1. **从小开始**: 建议从 0.01 (1%) 开始测试
2. **资金管理**: 确保账户有足够资金支撑最大仓位
3. **风险控制**: 结合止损和最大回撤设置使用
4. **杠杆同步**: 系统会自动复制目标地址的杠杆设置
5. **精度问题**: 若日志出现 `Order has invalid size.`，通常是该币种 `szDecimals` 要求更严格（例如必须整数），需要确保程序拿到了正确的 `szDecimals` 并对数量做了向下取整。

## 常见问题

**Q: 跟单比例设置为1.0是否意味着完全复制？**
A: 是的，1.0表示完全复制目标的交易大小，但仍受最大仓位限制。

**Q: 如果目标交易很小，跟单大小会怎样？**
A: 如果计算出的跟单大小小于最小交易要求(0.01)，系统会跳过该交易。

**Q: 平仓时跟单比例怎么计算？**
A: 平仓时也使用同样的比例逻辑，确保仓位比例保持一致。