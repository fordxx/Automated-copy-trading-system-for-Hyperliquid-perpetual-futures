[pytest]
# Only collect unit tests under ./tests.
# The repository also contains runnable scripts named test_*.py (e.g. test_telegram.py)
# which are not intended for pytest collection and may conflict by module name.
testpaths = tests
python_files = test_*.py
addopts = -ra
